@page "/msgwhatsapp"

@inject IWhastAppServicio _whatsapp

<h3>Enviar Mensaje a número de WhastApp registrado</h3>

<div class="row justify-content-center  mt-3">
    <div class="col-sm-12 col-md-8 col-xl-10">
        <EditForm Model="modelo" OnValidSubmit="EnviarMesaje">
            <DataAnnotationsValidator></DataAnnotationsValidator>

            <div class="input-group mt-3">
                <span class="input-group-text  bg-secondary text-light" id="basic-addon3">Identificación</span>
                <input type="text" class="form-control" @bind-value="modelo.DeNumero">
            </div>
            <ValidationMessage For="@(()=>modelo.DeNumero)" />
            <div class="input-group mt-3">
                <span class="input-group-text  bg-secondary text-light" id="basic-addon3">Nombres</span>
                <input type="text" class="form-control" @bind-value="modelo.ParaNumero">
            </div>
            <ValidationMessage For="@(()=>modelo.ParaNumero)" />
            <div class="input-group mt-3">
                <span class="input-group-text  bg-secondary text-light" id="basic-addon3">Apellidos</span>
                <input type="text" class="form-control" @bind-value="modelo.Mensaje">
            </div>
            <ValidationMessage For="@(()=>modelo.Mensaje)" />
        </EditForm>
    </div>
</div>
@code {
    private MensajeWhastAppDTO modelo = new MensajeWhastAppDTO();
    private async Task EnviarMesaje()
    {
        
    }

}
